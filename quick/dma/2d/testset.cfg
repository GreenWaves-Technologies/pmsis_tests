from plptest import *

TestConfig = c = {}

test0 = Test(
  name = '2d_stride_0x8000',
  commands = [
    Shell('clean', 'make clean'),
    Shell('build', 'make all STRIDE=0x8000'),
    Shell('run',   'make run STRIDE=0x8000')
  ],
  timeout=1000000
)
  
test1 = Test(
  name = '2d_stride_0x11000',
  commands = [
    Shell('clean', 'make clean'),
    Shell('build', 'make all STRIDE=0x11000'),
    Shell('run',   'make run STRIDE=0x11000')
  ],
  timeout=1000000
)
  
test2 = Test(
  name = '2d',
  commands = [
    Shell('clean', 'make clean'),
    Shell('build', 'make all'),
    Shell('run',   'make run')
  ],
  timeout=1000000
)
  
c['tests'] = [ test0, test1, test2 ]
