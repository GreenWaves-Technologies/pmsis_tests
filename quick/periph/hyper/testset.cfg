from plptest import *

TestConfig = c = {}

test = Testset(
  name  = 'hyper',
  files = [
    'ram/testset.cfg'
  ],
  restrict = '"hyper" in config.get("**/udma/interfaces").get_dict() and not config.get_str("**/chip/name") in ["arnold"]'
)

c['testsets'] = [ test ]
