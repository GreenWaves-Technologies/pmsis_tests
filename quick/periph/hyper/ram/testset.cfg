from plptest import *

TestConfig = c = {}

test = Testset(
  name  = 'ram',
  files = [
    'simple/testset.cfg',
    'multiple/testset.cfg'
  ],
  restrict = '"hyper" in config.get("**/udma/interfaces").get_dict()'
)

c['testsets'] = [ test ]
