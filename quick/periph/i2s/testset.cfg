from plptest import *

TestConfig = c = {}

test = Testset(
  name  = 'i2s',
  files = [
    'continuous/testset.cfg',
    'simple/testset.cfg',
    'start_stop/testset.cfg'
  ],
  restrict = 'os.environ.get("PLPTEST_PLATFORM") != "board" and (os.environ.get("TARGET_CHIP_FAMILY") == "GAP8" or config.get_str("**/chip/pulp_chip_family") == "gap")'
)

c['testsets'] = [ test ]
