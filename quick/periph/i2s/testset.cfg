from plptest import *

TestConfig = c = {}

test = Testset(
  name  = 'i2s',
  files = [
    'simple/testset.cfg'
  ],
  restrict = '"i2s" in config.get("**/udma/interfaces").get_dict()'
)

c['testsets'] = [ test ]
