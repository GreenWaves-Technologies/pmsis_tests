from plptest import *

TestConfig = c = {}

test = Test(
  name = 'i2c_stress_test',
  commands = [
    Shell('clean', 'make clean'),
    Shell('build', 'make all'),
    Shell('run',   'make run')
  ],
  timeout=300,
  restrict="os.environ.get('PLATFORM') == 'fpga'"
)

c['tests'] = [ test ]
