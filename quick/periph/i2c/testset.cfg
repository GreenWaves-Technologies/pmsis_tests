from plptest import *
import os

TestConfig = c = {}

tests = []

if os.environ.get('TARGET_CHIP') == 'GAP9_V2':
  tests = [
    'bench_tests/testset.cfg',
    'stress_tests/testset.cfg',
    'unit_tests/testset.cfg',
  ]

test = Testset(
  name  = 'i2c',
  files = tests
)

c['testsets'] = [ test ]
