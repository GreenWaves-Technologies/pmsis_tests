from plptest import *
import os

TestConfig = c = {}

c['tests'] = []

if os.environ.get('GAPY_PLATFORM') == 'rtl' and os.environ.get('TARGET_CHIP') == 'GAP9_V2':
  pass
else:
  c['tests'].append(Test(name='echo',
                         commands = [ Shell('echo', 'echo TOTO' ) ],
                         timeout = 1000000
                        )
                   )
  c['tests'].append(Test(name='make-fail',
                         commands = [ Shell('make-fail', 'make clean' ) ],
                         timeout = 1000000
                        )
                   )
