from plptest import *

TestConfig = c = {}

test = Test(
  name = 'fork',
  commands = [
    Shell('clean', 'make clean'),
    Shell('build', 'make all'),
    Shell('run',   'make run')
  ],
  timeout=1000000,
  restrict='config.get("**/fc") is not None'
)
  
c['tests'] = [ test ]
